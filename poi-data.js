/* This placeholder is replaced by the first scheduled/manual map-data update.
   Until then app.js deliberately falls back to the existing live/authoritative
   Matching POI loaders, so deploying this file cannot remove any POIs. */
window.AALBORG_POI_DATA = {"version":1,"ready":false,"generatedAt":null,"categories":{}};
