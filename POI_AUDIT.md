# Aalborg POI bundle audit

The bundled POI snapshot has not been generated yet.

Run **Actions → Update Aalborg map data and deploy Pages → Run workflow** once after deploying this version. The workflow will download the current Geofabrik Denmark OSM extract, generate `poi-data.js`, validate it, commit it, and deploy the result.
